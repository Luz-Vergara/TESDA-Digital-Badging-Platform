import { useEffect, useMemo, useState } from 'react'; import { Link } from 'react-router-dom'; import { CheckCircle2, Search } from 'lucide-react'; import { externalApi, ExternalApiError } from '@/src/services/externalApi'; import type { ExternalLearnerSummary } from '@/src/types/external-api'; import { Badge } from '@/components/ui/badge'; import { Button } from '@/components/ui/button'; import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
export default function BadgeEligibility(){const [learners,setLearners]=useState<ExternalLearnerSummary[]>([]),[term,setTerm]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);useEffect(()=>{void(async()=>{try{setLearners((await externalApi.getMyTrainingCenterLearners()).data)}catch(e){setError(e instanceof ExternalApiError?e.message:'Unable to load external eligibility.')}finally{setLoading(false)}})()},[]);const rows=useMemo(()=>learners.flatMap(l=>l.badgeEligibility.map(e=>({l,e}))).filter(({l,e})=>`${l.displayName} ${l.learnerUli} ${e.ctprNumber}`.toLowerCase().includes(term.toLowerCase())),[learners,term]);if(loading)return <div className="min-h-[60vh] flex items-center justify-center"><div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"/></div>;return <div className="space-y-6"><div><div className="flex gap-2 items-center"><h1 className="text-2xl font-bold">Badge Eligibility</h1><Badge className="bg-slate-100 text-slate-700 border-slate-200">Source: External MIS</Badge></div><p className="text-sm text-slate-500 mt-1">Eligibility is calculated from scoped external enrollment and competency evidence.</p></div>{error&&<Card className="border-rose-200"><CardContent className="p-4 text-rose-700 text-sm">{error}</CardContent></Card>}<Card><CardContent className="p-4"><div className="relative max-w-md"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/><Input className="pl-9" value={term} onChange={e=>setTerm(e.target.value)} placeholder="Search learner, ULI, or CTPR..."/></div></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Learner Qualification & Eligibility Roster</CardTitle><CardDescription>Only eligible enrollments can create the immutable Firestore request snapshot.</CardDescription></CardHeader><CardContent className="p-0"><Table><TableHeader><TableRow className="bg-slate-50"><TableHead>Learner / ULI</TableHead><TableHead>CTPR</TableHead><TableHead>Competencies</TableHead><TableHead>Result</TableHead><TableHead/></TableRow></TableHeader><TableBody>{rows.map(({l,e})=><TableRow key={e.id}><TableCell><p className="font-semibold">{l.displayName}</p><p className="font-mono text-xs">{l.learnerUli}</p></TableCell><TableCell>{e.ctprNumber}</TableCell><TableCell>{e.completedCompetencyCount}/{e.requiredCompetencyCount}{e.missingCompetencyCodes.length? <p className="text-xs text-amber-700">Missing: {e.missingCompetencyCodes.join(', ')}</p>:null}</TableCell><TableCell><Badge className={e.eligible?'bg-emerald-100 text-emerald-800':'bg-amber-100 text-amber-800'}>{e.eligible?'Eligible':'Not yet eligible'}</Badge></TableCell><TableCell>{e.eligible?<Link to={`/trainingcenter/file-request?uli=${encodeURIComponent(l.learnerUli)}&enrollment=${encodeURIComponent(e.enrollmentId)}`}><Button size="sm"><CheckCircle2 className="h-4 w-4 mr-1"/>File request</Button></Link>:<span className="text-xs text-slate-400">Complete requirements</span>}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div>}
